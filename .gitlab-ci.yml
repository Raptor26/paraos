stages:
 - test_linux_posix_debug
 - test_linux_posix_release
 - test_linux_freertos_debug
 - test_linux_freertos_release
 - test_windows_winapi_debug
 - test_windows_winapi_release
 - test_windows_freertos_debug
 - test_windows_freertos_release

## Linux -----------------------------------------------------------------------
ci:linux_posix_debug:
 stage: test_linux_posix_debug
 needs: []
 tags:
  - linux
 script:
  - cmake --preset pc_debug_clang
  - cmake --build build/pc_debug_clang
  - ctest --preset test_pc_debug_clang

ci:linux_posix_release:
 stage: test_linux_posix_release
 needs: []
 tags:
  - linux
 script:
  - cmake --preset pc_release_clang
  - cmake --build build/pc_release_clang
  - ctest --preset test_pc_release_clang

ci:linux_freertos_debug:
 stage: test_linux_freertos_debug
 needs: []
 tags:
  - linux
 script:
  - cmake --preset freertos_debug_clang
  - cmake --build build/freertos_debug_clang
  - ctest --preset test_freertos_debug_clang

ci:linux_freertos_release:
 stage: test_linux_freertos_release
 needs: []
 tags:
  - linux
 script:
  - cmake --preset freertos_release_clang
  - cmake --build build/freertos_release_clang
  - ctest --preset test_freertos_release_clang

## windows ---------------------------------------------------------------------
ci:windows:
 stage: test_windows_winapi_debug
 needs: []
 tags:
  - windows
 script:
  - cmake --preset pc_debug_clang
  - cmake --build --preset build_pc_debug_clang
  - ctest --preset test_pc_debug_clang

ci:windows_winapi_release:
 stage: test_windows_winapi_release
 needs: []
 tags:
  - linux
 script:
  - cmake --preset pc_release_clang
  - cmake --build build/pc_release_clang
  - ctest --preset test_pc_release_clang

ci:windows_freertos:
 stage: test_windows_freertos_debug
 needs: []
 tags:
  - linux
 script:
  - cmake --preset freertos_debug_clang
  - cmake --build build/freertos_debug_clang
  - ctest --preset test_freertos_debug_clang

ci:windows_freertos_release:
 stage: test_windows_freertos_release
 needs: []
 tags:
  - linux
 script:
  - cmake --preset freertos_release_clang
  - cmake --build build/freertos_release_clang
  - ctest --preset test_freertos_release_clang