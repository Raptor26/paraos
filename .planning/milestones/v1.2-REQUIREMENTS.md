# Requirements: v1.2 std::jthread-style Thread API

## Scope

Добавить в PARAOS новый публичный API `paraos::jthread`, совместимый по духу с `std::jthread` из C++20. API должен быть единым для Windows, Unix и FreeRTOS: пользовательский код, использующий `paraos::jthread`, компилируется без изменений под любой платформой.

Старый `paraos::Thread` остаётся нетронутым — замена поэтапная. Новый API вводится рядом со старым.

## Requirements

### JTHREAD — Core API

- [x] **JTHREAD-01**: `paraos::jthread` конструируется из callable и произвольного числа аргументов.
  - `paraos::stop_token` передаётся в callable последним аргументом.
  - Поддерживаются capturing lambdas, free functions, member functions через `std::function`-подобный механизм на FreeRTOS и `std::jthread` на PC.
- [x] **JTHREAD-02**: `paraos::stop_token` предоставляет `stop_requested()` и `stop_possible()`.
- [x] **JTHREAD-03**: `paraos::jthread::request_stop()` запрашивает остановку потока.
- [x] **JTHREAD-04**: `paraos::jthread::join()` блокирует вызывающий поток до завершения целевого потока.
- [x] **JTHREAD-05**: Деструктор `paraos::jthread` автоматически вызывает `request_stop()` и `join()`, если поток joinable.
- [x] **JTHREAD-06**: `paraos::jthread` поддерживает move-семантику, но не копирование.

### JTHREAD — Thread Attributes

- [x] **JTHREAD-07**: Конструктор `paraos::jthread` принимает `ThreadAttr` (имя, размер стека, приоритет) наряду с callable и аргументами.
- [x] **JTHREAD-08**: На FreeRTOS `ThreadAttr::priority` задаёт приоритет задачи FreeRTOS.
- [x] **JTHREAD-09**: На PC `ThreadAttr::priority` обрабатывается через платформенный API (`pthread_setschedparam` на Unix, `SetThreadPriority` на Windows) или игнорируется, если платформа не позволяет его задать без привилегий.
- [x] **JTHREAD-10**: На FreeRTOS `ThreadAttr::stack_depth` и `thread_name` передаются в `xTaskCreate`.

### Portability

- [x] **PORT-01**: `port_pc/paraos_jthread.hpp` содержит единую реализацию `paraos::jthread` для Windows и Unix поверх `std::jthread`.
- [x] **PORT-02**: `port_unix/paraos_jthread.hpp` включает `port_pc/paraos_jthread.hpp`.
- [x] **PORT-03**: `port_win/paraos_jthread.hpp` включает `port_pc/paraos_jthread.hpp`.
- [x] **PORT-04**: `port_freertos/paraos_jthread.hpp` содержит собственную реализацию `paraos::jthread` поверх FreeRTOS API.
- [x] **PORT-05**: Пользовательский код не должен содержать платформенных `#ifdef` для выбора между `jthread`-реализациями.

### Build & Tests

- [x] **BUILD-01**: Минимальная версия C++ повышена до 20 в корневом `CMakeLists.txt`.
- [x] **BUILD-02**: `paraos_jthread.hpp` доступен для включения из всех портов.
- [x] **TEST-01**: Тест `port_tests/test_jthread_basic.cpp` компилируется и проходит на `pc_debug_clang`.
- [x] **TEST-02**: Тест `port_tests/test_jthread_basic.cpp` компилируется и проходит на `pc_debug_gcc`.
- [x] **TEST-03**: Тест `port_tests/test_jthread_basic.cpp` компилируется и проходит на `freertos_debug_clang`.
- [x] **TEST-04**: Тест `port_tests/test_jthread_basic.cpp` компилируется и проходит на `freertos_debug_gcc`.
- [x] **TEST-05**: `*_clang_tidy` пресеты продолжают собираться без новых предупреждений.

## Out of Scope

- Полная замена `paraos::Thread` на `paraos::jthread` — в этой вехе новый API добавляется рядом со старым.
- Миграция `extra/` и `containers/` на `jthread` — откладывается на будущие вехи.
- Добавление новых портов (macOS, Zephyr и т.д.) — в этой вехе поддерживаются только Windows, Unix, FreeRTOS.
- `std::stop_callback`-совместимый API — в минимальной версии не требуется.
- Полная бинарная совместимость с `std::jthread` (например, `get_id`, `detach`, `hardware_concurrency`) — только то, что нужно для целевого теста и базового использования.

## Traceability

| Requirement | Phase | Plan |
|-------------|-------|------|
| JTHREAD-01  | 9     | 09-01 |
| JTHREAD-02  | 9     | 09-01 |
| JTHREAD-03  | 9     | 09-01 |
| JTHREAD-04  | 9     | 09-01 |
| JTHREAD-05  | 9     | 09-01 |
| JTHREAD-06  | 9     | 09-01 |
| JTHREAD-07  | 11    | 11-01 |
| JTHREAD-08  | 11    | 11-01 |
| JTHREAD-09  | 11    | 11-01 |
| JTHREAD-10  | 11    | 11-01 |
| PORT-01     | 9     | 09-01 |
| PORT-02     | 9     | 09-01 |
| PORT-03     | 9     | 09-01 |
| PORT-04     | 10    | 10-01 |
| PORT-05     | 9     | 09-01 |
| BUILD-01    | 12    | 12-01 |
| BUILD-02    | 9     | 09-01 |
| TEST-01     | 12    | 12-01 |
| TEST-02     | 12    | 12-01 |
| TEST-03     | 12    | 12-01 |
| TEST-04     | 12    | 12-01 |
| TEST-05     | 12    | 12-01 |
