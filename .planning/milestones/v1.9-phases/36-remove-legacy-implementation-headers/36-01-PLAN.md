---
phase: 36-remove-legacy-implementation-headers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - port_unix/paraos_thread.hpp
  - port_unix/paraos_mutex.hpp
  - port_unix/paraos_semaphore.hpp
  - port_win/paraos_thread.hpp
  - port_win/paraos_mutex.hpp
  - port_win/paraos_semaphore.hpp
  - port_freertos/paraos_thread.hpp
  - port_freertos/paraos_mutex.hpp
  - port_freertos/paraos_semaphore.hpp
  - paraos_thread_common.hpp
  - paraos_mutex_raii.hpp
  - CMakeLists.txt
autonomous: true
requirements:
  - REM-01
  - REM-02
  - REM-03
  - REM-04
  - REM-05
user_setup: []
must_haves:
  truths:
    - Legacy Thread/Mutex/Semaphore implementation headers are deleted from all ports.
    - paraos_thread_common.hpp still compiles and provides ThreadAttr and ThreadPriority.
    - paraos_mutex_raii.hpp no longer references MutexBase.
    - CMake configure succeeds after header removals.
  artifacts:
    - path: paraos_thread_common.hpp
      provides: Shared thread attributes and priority enum used by jthread.
      contains: "struct ThreadAttr"
    - path: paraos_mutex_raii.hpp
      provides: RAII lock guard using std-like paraos::mutex.
      contains: "std::lock_guard"
  key_links: []
---

<objective>
Remove legacy `paraos::Thread`, `paraos::Mutex`, and `paraos::Semaphore*` implementation headers from all three ports, clean `paraos_thread_common.hpp`, and rewrite `paraos_mutex_raii.hpp` to use std-like locking.
</objective>

<execution_context>
@$HOME/.kimi-code/gsd-core/workflows/execute-plan.md
@$HOME/.kimi-code/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-remove-legacy-implementation-headers/36-CONTEXT.md

@paraos_thread_common.hpp
@paraos_mutex_raii.hpp
</context>

<tasks>

<task type="auto">
  <name>Audit remaining legacy header includes</name>
  <files>.</files>
  <action>Search the repository (excluding build/, leaf/, etl/, GSL/, lwrb/, FreeRTOS-Kernel/) for any remaining `#include "paraos_thread.hpp"`, `#include "paraos_mutex.hpp"`, `#include "paraos_semaphore.hpp"`, or `#include "paraos_mutex_raii.hpp"`. Record every file that still references them so downstream phases know what to migrate.</action>
  <verify>grep -rln '#include "paraos_thread\.hpp"\|#include "paraos_mutex\.hpp"\|#include "paraos_semaphore\.hpp"' --include="*.hpp" --include="*.cpp" --include="*.h" . | grep -v build/ | grep -vE '^(leaf|etl|GSL|lwrb|port_freertos/FreeRTOS-Kernel)/'</verify>
  <acceptance_criteria>
    - Audit list is saved or reproducible via the verify command.
  </acceptance_criteria>
  <done>All remaining legacy include sites are identified.</done>
</task>

<task type="auto">
  <name>Clean paraos_thread_common.hpp</name>
  <files>paraos_thread_common.hpp</files>
  <read_first>paraos_thread_common.hpp</read_first>
  <action>Remove the `dtor_callback` and `run_` members from `ThreadAttr`, plus any related comments marked for removal after the jthread transition. Keep `thread_name`, `stack_depth`, `priority`, and the `ThreadPriority` enum. Update or remove the comments that mention the legacy transition. Ensure the header still compiles standalone.</action>
  <verify>g++ -std=c++20 -I. -c -x c++ paraos_thread_common.hpp -o /dev/null</verify>
  <acceptance_criteria>
    - `paraos_thread_common.hpp` no longer contains `dtor_callback` or `run_`.
    - The header compiles with `-std=c++20`.
  </acceptance_criteria>
  <done>ThreadAttr cleaned of legacy-only fields.</done>
</task>

<task type="auto">
  <name>Rewrite paraos_mutex_raii.hpp</name>
  <files>paraos_mutex_raii.hpp</files>
  <read_first>paraos_mutex_raii.hpp,paraos_mutex_std.hpp</read_first>
  <action>Replace `MutexGuard` with a type alias or thin wrapper around `std::lock_guard<paraos::mutex>`. Since `paraos::mutex` is compatible with `std::lock_guard`/`std::unique_lock`, the simplest correct implementation is to provide a `MutexGuard` alias or remove the header entirely. To preserve any existing include sites during migration, keep the header and define `MutexGuard` as `std::lock_guard<paraos::mutex>`. Remove the `MutexBase` include and add `<mutex>` plus `paraos_mutex.hpp` (which now resolves to the std-like `paraos::mutex`).</action>
  <verify>grep -L "MutexBase" paraos_mutex_raii.hpp && grep "std::lock_guard" paraos_mutex_raii.hpp</verify>
  <acceptance_criteria>
    - `paraos_mutex_raii.hpp` no longer references `MutexBase`.
    - `MutexGuard` is defined in terms of `std::lock_guard<paraos::mutex>`.
  </acceptance_criteria>
  <done>MutexGuard rewritten to use std-like mutex.</done>
</task>

<task type="auto">
  <name>Delete legacy implementation headers</name>
  <files>
    port_unix/paraos_thread.hpp
    port_unix/paraos_mutex.hpp
    port_unix/paraos_semaphore.hpp
    port_win/paraos_thread.hpp
    port_win/paraos_mutex.hpp
    port_win/paraos_semaphore.hpp
    port_freertos/paraos_thread.hpp
    port_freertos/paraos_mutex.hpp
    port_freertos/paraos_semaphore.hpp
  </files>
  <action>Delete the ten legacy implementation headers listed above. If any CMake source list explicitly references them, remove those references as well.</action>
  <verify>test ! -f port_unix/paraos_thread.hpp && test ! -f port_unix/paraos_mutex.hpp && test ! -f port_unix/paraos_semaphore.hpp && test ! -f port_win/paraos_thread.hpp && test ! -f port_win/paraos_mutex.hpp && test ! -f port_win/paraos_semaphore.hpp && test ! -f port_freertos/paraos_thread.hpp && test ! -f port_freertos/paraos_mutex.hpp && test ! -f port_freertos/paraos_semaphore.hpp</verify>
  <acceptance_criteria>
    - All ten legacy headers are deleted.
    - No CMakeLists.txt references the deleted files.
  </acceptance_criteria>
  <done>Legacy headers deleted.</done>
</task>

</tasks>

<verification>
- [ ] Audit command returns only expected references (Phase 37 consumers).
- [ ] `paraos_thread_common.hpp` compiles standalone with C++20.
- [ ] `paraos_mutex_raii.hpp` compiles and does not reference `MutexBase`.
- [ ] All ten legacy headers are deleted.
</verification>

<success_criteria>
- All tasks completed.
- Legacy headers deleted from all ports.
- `paraos_thread_common.hpp` still provides `ThreadAttr`, `ThreadPriority`, and shared definitions.
- `paraos_mutex_raii.hpp` uses std-like locking.
- No accidental deletion of non-legacy files.
</success_criteria>

<output>
After completion, create `.planning/phases/36-remove-legacy-implementation-headers/36-01-SUMMARY.md`.
</output>
