# Requirements Archive: PARAOS v1.7

**Milestone:** v1.7 — Migrate `test_thread_only_*` to `paraos::jthread`  
**Shipped:** 2026-06-23  
**Status:** Complete

## v1.7 Requirements — Final Status

### Test Migration

- [x] **MIG-01**: `test_thread_only_stack.cpp` переведён с `paraos::Thread` на `paraos::jthread`.
- [x] **MIG-02**: `test_thread_only_stack_with_multiple_threads.cpp` переведён с `paraos::Thread` на `paraos::jthread`.
- [x] **MIG-03**: `test_thread_only_static.cpp` переведён с `paraos::Thread` на `paraos::jthread`.
- [x] **MIG-04**: `test_thread_only_global.cpp` переведён с `paraos::Thread` на `paraos::jthread`.

### Cross-Platform Lifecycle

- [x] **LIFE-01**: Во всех четырёх тестах используется единый паттерн запуска/завершения: `paraos::jthread::start_scheduler()`, `IdleHook()` → `WaitForSchedulerEnded()` → `CheckIfTestSuccessfullyComplete()` → `paraos::jthread::end_scheduler()`.
- [x] **LIFE-02**: Для FreeRTOS-пути устанавливается `paraos::freertos_idle_fnc_ptr = IdleHook;` под `#if PARAOS_LIKE_FREERTOS`.
- [x] **LIFE-03**: Удалены платформенные ветви `std::_Exit()`, `paraos::Thread::Exit()`, `paraos::Thread::DeleteAll()` и ручной `new`/`delete` потоков там, где это возможно.

### Idiomatic C++

- [x] **IDIO-01**: Создание и удаление потоков выполнено идиоматично для C++: RAII-контейнеры (`std::vector<paraos::jthread>`), `std::jthread`-стиль лямбд с `paraos::stop_token`, автоматическое объединение/остановка через деструкторы.

### Build & Static Analysis

- [x] **BUILD-01**: `port_tests/CMakeLists.txt` переключил четыре цели `test_thread_only_*` на `cxx_std_20`.
- [x] **BUILD-02**: Четыре цели `test_thread_only_*` включены в `CXX_CLANG_TIDY` при `CLANG_TIDY_ENABLE`.
- [x] **BUILD-03**: Каждый тест зарегистрирован в CTest с таймаутом 20 секунд.
- [x] **BUILD-04**: Все доступные CMake-пресеты на macOS конфигурируются и собираются без новых предупреждений clang-tidy.

### Test Execution

- [x] **TEST-01**: `ctest` для PC-пресетов (`pc_debug_clang`, `pc_debug_gcc`) на macOS проходит полностью в пределах таймаута 20 секунд.
- [x] **TEST-02**: FreeRTOS-пресеты (`freertos_debug_clang`, `freertos_debug_gcc`) на macOS не только собираются, но и успешно выполняются и завершаются в пределах 20 секунд.
- [x] **TEST-03**: Код остаётся кроссплатформенным для Windows и Linux; регрессии в платформенных путях отсутствуют.

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| MIG-01 | 28 | ✅ Complete |
| MIG-02 | 28 | ✅ Complete |
| MIG-03 | 28 | ✅ Complete |
| MIG-04 | 28 | ✅ Complete |
| LIFE-01 | 28 | ✅ Complete |
| LIFE-02 | 28 | ✅ Complete |
| LIFE-03 | 28 | ✅ Complete |
| IDIO-01 | 28 | ✅ Complete |
| BUILD-01 | 29 | ✅ Complete |
| BUILD-02 | 29 | ✅ Complete |
| BUILD-03 | 29 | ✅ Complete |
| BUILD-04 | 29 | ✅ Complete |
| TEST-01 | 30 | ✅ Complete |
| TEST-02 | 30 | ✅ Complete |
| TEST-03 | 30 | ✅ Complete |

**Coverage:** 15/15 v1.7 requirements satisfied.

## Notes

- `BUILD-04` дублируется в разделе Build Verification исходного REQUIREMENTS.md; засчитан один раз.
- Изменения ограничены `port_tests/test_thread_only_*.cpp` и `port_tests/CMakeLists.txt`; платформенные пути `port_unix/`, `port_win/`, `port_freertos/` не затронуты.
