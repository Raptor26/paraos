---
phase: 37-migrate-internal-consumers-to-std-like-primitives
plan: 01
type: execute
wave: 1
depends_on:
  - "36-01"
files_modified:
  - containers/paraos_message_buffer.hpp
  - containers/paraos_queue_blocking.hpp
  - port_unix/paraos_critical.hpp
  - port_unix/paraos_socket_udp.hpp
  - port_win/paraos_socket_udp.hpp
  - port_pc/paraos_recursive_mutex_std.hpp
  - port_unix/paraos_recursive_mutex_std.hpp
  - port_win/paraos_recursive_mutex_std.hpp
  - port_freertos/paraos_recursive_mutex.hpp
  - CMakeLists.txt
autonomous: true
requirements:
  - MIG-01
  - MIG-02
  - MIG-03
  - MIG-04
user_setup: []
must_haves:
  truths:
    - containers/paraos_message_buffer.hpp and paraos_queue_blocking.hpp use paraos::mutex and paraos::*_semaphore.
    - port_unix/paraos_critical.hpp uses a recursive mutex and no longer references MutexBase.
    - port_unix/paraos_socket_udp.hpp and port_win/paraos_socket_udp.hpp use paraos::sleep_for instead of paraos::Thread::DelayMs.
    - All PC and FreeRTOS presets still configure after migration.
  artifacts:
    - path: containers/paraos_queue_blocking.hpp
      provides: Blocking queue using std-like mutex and semaphore.
      contains: "paraos::mutex"
    - path: port_unix/paraos_critical.hpp
      provides: Recursive critical section using std-like recursive mutex.
      contains: "paraos::recursive_mutex"
    - path: port_unix/paraos_socket_udp.hpp
      provides: UDP socket implementation using paraos::sleep_for.
      contains: "paraos::sleep_for"
  key_links:
    - from: port_unix/paraos_critical.hpp
      to: port_pc/paraos_recursive_mutex_std.hpp
      via: "#include \"paraos_recursive_mutex_std.hpp\""
      pattern: "paraos_recursive_mutex"
---

<objective>
Update remaining internal consumers of legacy `paraos::Thread`, `paraos::Mutex`, and `paraos::Semaphore*` to use the std-like replacements.
</objective>

<execution_context>
@$HOME/.kimi-code/gsd-core/workflows/execute-plan.md
@$HOME/.kimi-code/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-migrate-internal-consumers-to-std-like-primitives/37-CONTEXT.md

@containers/paraos_message_buffer.hpp
@containers/paraos_queue_blocking.hpp
@port_unix/paraos_critical.hpp
@port_unix/paraos_socket_udp.hpp
@port_win/paraos_socket_udp.hpp
@paraos_mutex_std.hpp
@paraos_semaphore_std.hpp
@paraos_thread_common.hpp
</context>

<tasks>

<task type="auto">
  <name>Introduce paraos::recursive_mutex</name>
  <files>
    port_pc/paraos_recursive_mutex_std.hpp
    port_unix/paraos_recursive_mutex_std.hpp
    port_win/paraos_recursive_mutex_std.hpp
    port_freertos/paraos_recursive_mutex.hpp
  </files>
  <read_first>port_pc/paraos_mutex_std.hpp,port_freertos/paraos_mutex_std.hpp</read_first>
  <action>Create a `paraos::recursive_mutex` primitive mirroring `std::recursive_mutex` semantics. For PC/Unix/Windows, create `port_pc/paraos_recursive_mutex_std.hpp` wrapping `std::recursive_mutex`, and forwarding headers in `port_unix/` and `port_win/`. For FreeRTOS, create `port_freertos/paraos_recursive_mutex.hpp` using `xSemaphoreCreateRecursiveMutex()`. Ensure the type is non-copyable/non-movable and provides `lock()`, `try_lock()`, and `unlock()`.</action>
  <verify>grep -l "recursive_mutex" port_pc/paraos_recursive_mutex_std.hpp port_unix/paraos_recursive_mutex_std.hpp port_win/paraos_recursive_mutex_std.hpp port_freertos/paraos_recursive_mutex.hpp</verify>
  <acceptance_criteria>
    - `paraos::recursive_mutex` is defined in all four port locations.
    - Type is non-copyable, non-movable, and exposes `lock()`, `try_lock()`, `unlock()`.
  </acceptance_criteria>
  <done>Recursive mutex primitive available on all ports.</done>
</task>

<task type="auto">
  <name>Migrate containers to std-like primitives</name>
  <files>containers/paraos_message_buffer.hpp,containers/paraos_queue_blocking.hpp</files>
  <read_first>containers/paraos_message_buffer.hpp,containers/paraos_queue_blocking.hpp,paraos_mutex_raii.hpp,paraos_semaphore_std.hpp</read_first>
  <action>Replace legacy `MutexGuard`/`MutexBase` usage with `std::lock_guard<paraos::mutex>` or `std::unique_lock<paraos::mutex>`. Replace legacy semaphore usage with `paraos::counting_semaphore` or `paraos::binary_semaphore` as appropriate. Update `#include` directives to use `paraos_mutex.hpp` (std-like) and `paraos_semaphore.hpp` (std-like). Remove `#include "paraos_mutex_raii.hpp"` if no longer needed.</action>
  <verify>grep -L "MutexBase\|SemaphoreBase\|paraos_mutex_raii" containers/paraos_message_buffer.hpp containers/paraos_queue_blocking.hpp && grep "paraos::mutex" containers/paraos_queue_blocking.hpp</verify>
  <acceptance_criteria>
    - Neither container header references `MutexBase`, `SemaphoreBase`, or `paraos_mutex_raii.hpp`.
    - `paraos::mutex` and `paraos::*_semaphore` are used for synchronization.
  </acceptance_criteria>
  <done>Container headers migrated to std-like primitives.</done>
</task>

<task type="auto">
  <name>Migrate port_unix/paraos_critical.hpp</name>
  <files>port_unix/paraos_critical.hpp</files>
  <read_first>port_unix/paraos_critical.hpp,port_pc/paraos_recursive_mutex_std.hpp</read_first>
  <action>Replace the static `MutexRecursive` member with a static `paraos::recursive_mutex`. Update constructor/destructor and `ForceEnter`/`ForceExit` to call `lock()`/`unlock()` on the recursive mutex. Remove the `#include "paraos_mutex.hpp"` (legacy) and include the new recursive mutex header plus any needed paraos utilities. Preserve the existing ISR template parameter and signatures.</action>
  <verify>grep -L "MutexBase\|MutexRecursive" port_unix/paraos_critical.hpp && grep "paraos::recursive_mutex" port_unix/paraos_critical.hpp</verify>
  <acceptance_criteria>
    - `port_unix/paraos_critical.hpp` no longer references `MutexBase` or `MutexRecursive`.
    - It uses `paraos::recursive_mutex`.
  </acceptance_criteria>
  <done>Unix critical section migrated to recursive mutex.</done>
</task>

<task type="auto">
  <name>Migrate socket UDP delay to paraos::sleep_for</name>
  <files>port_unix/paraos_socket_udp.hpp,port_win/paraos_socket_udp.hpp</files>
  <read_first>port_unix/paraos_socket_udp.hpp,port_win/paraos_socket_udp.hpp,paraos_thread_common.hpp</read_first>
  <action>Replace `paraos::Thread::DelayMs(connection_waiting_delay_ms_)` with `paraos::sleep_for(std::chrono::milliseconds(connection_waiting_delay_ms_))`. Add `#include <chrono>` and include `paraos_thread_common.hpp` or the appropriate utility header that defines `paraos::sleep_for`. Remove any `#include "paraos_thread.hpp"`.</action>
  <verify>grep -L "paraos::Thread::DelayMs\|#include \"paraos_thread\.hpp\"" port_unix/paraos_socket_udp.hpp port_win/paraos_socket_udp.hpp && grep "paraos::sleep_for" port_unix/paraos_socket_udp.hpp port_win/paraos_socket_udp.hpp</verify>
  <acceptance_criteria>
    - Neither socket UDP header references `paraos::Thread::DelayMs` or `#include "paraos_thread.hpp"`.
    - Both use `paraos::sleep_for(std::chrono::milliseconds(...))`.
  </acceptance_criteria>
  <done>Socket UDP delay migrated to sleep_for.</done>
</task>

<task type="auto">
  <name>Update CMake source lists</name>
  <files>CMakeLists.txt</files>
  <read_first>CMakeLists.txt</read_first>
  <action>If `CMakeLists.txt` explicitly lists the deleted legacy headers or the new recursive mutex headers, update the source list. Most headers are included via glob or `#include` paths, but verify no hard-coded references to deleted files remain and add recursive mutex headers if needed.</action>
  <verify>grep -nE "paraos_(thread|mutex|semaphore)\.hpp" CMakeLists.txt || true</verify>
  <acceptance_criteria>
    - No hard-coded references to deleted legacy headers in CMakeLists.txt.
  </acceptance_criteria>
  <done>CMake source lists updated.</done>
</task>

</tasks>

<verification>
- [ ] PC preset (`pc_debug_clang`) configures successfully.
- [ ] FreeRTOS preset (`freertos_debug_clang`) configures successfully.
- [ ] No references to `MutexBase` or `SemaphoreBase` remain in migrated internal headers.
</verification>

<success_criteria>
- All tasks completed.
- Internal consumers use std-like primitives.
- `paraos::recursive_mutex` available on all ports.
- CMake configures on PC and FreeRTOS.
</success_criteria>

<output>
After completion, create `.planning/phases/37-migrate-internal-consumers-to-std-like-primitives/37-01-SUMMARY.md`.
</output>
