---
phase: 39-build-static-analysis-and-regression-verification
plan: 01
type: execute
wave: 1
depends_on:
  - "38-01"
files_modified: []
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-03
  - BUILD-04
user_setup: []
must_haves:
  truths:
    - pc_debug_clang, pc_debug_gcc, and pc_debug_gcc_clang_tidy presets configure, build, and pass ctest.
    - freertos_debug_clang and freertos_debug_gcc presets compile.
    - *_clang_tidy presets produce no new warnings.
    - PC ctest count is at least 58 or documented rationale is provided.
  artifacts: []
  key_links: []
---

<objective>
Run full build, static analysis, and regression verification for all PC and FreeRTOS presets after legacy removal.
</objective>

<execution_context>
@$HOME/.kimi-code/gsd-core/workflows/execute-plan.md
@$HOME/.kimi-code/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-build-static-analysis-and-regression-verification/39-CONTEXT.md

@CMakePresets.json
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Configure and build PC presets</name>
  <files>.</files>
  <action>Run `cmake --preset pc_debug_clang`, `cmake --build build/pc_debug_clang/`, then `ctest --test-dir build/pc_debug_clang/ --output-on-failure --stop-on-failure`. Repeat for `pc_debug_gcc`. Record configure/build/test output and the final ctest count.</action>
  <verify>ctest --test-dir build/pc_debug_clang/ -N | tail -5</verify>
  <acceptance_criteria>
    - `pc_debug_clang` and `pc_debug_gcc` configure, build, and pass ctest.
    - ctest count is at least 58 or a rationale is recorded.
  </acceptance_criteria>
  <done>PC presets pass.</done>
</task>

<task type="auto">
  <name>Configure and build clang-tidy preset</name>
  <files>.</files>
  <action>Run `cmake --preset pc_debug_gcc_clang_tidy`, `cmake --build build/pc_debug_gcc_clang_tidy/`, and `ctest --test-dir build/pc_debug_gcc_clang_tidy/ --output-on-failure`. Confirm no new clang-tidy warnings are produced from changed/deleted code. Since deleted code cannot emit warnings, focus on remaining files.</action>
  <verify>cmake --build build/pc_debug_gcc_clang_tidy/ 2>&1 | grep -i "warning\|error" | head -20 || echo "no warnings/errors"</verify>
  <acceptance_criteria>
    - clang-tidy preset configures, builds, and passes tests without new warnings.
  </acceptance_criteria>
  <done>clang-tidy preset clean.</done>
</task>

<task type="auto">
  <name>Configure and build FreeRTOS presets</name>
  <files>.</files>
  <action>Run `cmake --preset freertos_debug_clang` and `cmake --build build/freertos_debug_clang/`. Repeat for `freertos_debug_gcc`. FreeRTOS presets are compile-only on this host; record any errors.</action>
  <verify>cmake --build build/freertos_debug_clang/ 2>&1 | tail -20</verify>
  <acceptance_criteria>
    - `freertos_debug_clang` and `freertos_debug_gcc` compile successfully.
  </acceptance_criteria>
  <done>FreeRTOS presets compile.</done>
</task>

<task type="auto">
  <name>Record verification results</name>
  <files>.planning/phases/39-build-static-analysis-and-regression-verification/39-VERIFICATION.md</files>
  <action>Write a `39-VERIFICATION.md` file summarizing the build/test results for each preset, the final PC ctest count, any deviations from the 58-test baseline with rationale, and the overall status (passed/gaps_found).</action>
  <verify>test -f .planning/phases/39-build-static-analysis-and-regression-verification/39-VERIFICATION.md && grep "^status:" .planning/phases/39-build-static-analysis-and-regression-verification/39-VERIFICATION.md</verify>
  <acceptance_criteria>
    - `39-VERIFICATION.md` exists and contains a `status:` line.
  </acceptance_criteria>
  <done>Verification recorded.</done>
</task>

</tasks>

<verification>
- [ ] All PC presets pass ctest.
- [ ] clang-tidy preset is clean.
- [ ] FreeRTOS presets compile.
- [ ] `39-VERIFICATION.md` is written with status.
</verification>

<success_criteria>
- All tasks completed.
- No regressions in PC or FreeRTOS builds.
- Static analysis remains clean.
- Verification artifacts are complete.
</success_criteria>

<output>
After completion, create `.planning/phases/39-build-static-analysis-and-regression-verification/39-01-SUMMARY.md`.
</output>
